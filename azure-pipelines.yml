trigger:
- main

pool:
  vmImage: default

variables:
- group: test

steps:
- script: $(wipath) -u https://google.com -r "Executive Summary" -y Standard -f $(Build.ArtifactStagingDirectory)\test12345.pdf -gp
  displayName: 'Run WebInspect'